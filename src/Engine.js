'use strict';

var paletto = function () {

    var board;
    var Marblescolor;

    var init=function(){

        board = new Array(6);
        for(i=0; i<6; i++){
            board[i]=new Array(6);
            for(j=0;j<6;j++){
                board[i][j]= -1;
            }
        }
    };

// private attributes and methods

// public methods
};
